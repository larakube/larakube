apiVersion: v1
kind: Service
metadata:
  name: {{ include "laravel-kubernetes.fullname" . }}
  labels:
    {{- include "laravel-kubernetes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "laravel-kubernetes.selectorLabels" . | nindent 4 }}
