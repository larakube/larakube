apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "laravel-kubernetes.fullname" . }}-test-connection"
  labels:
    {{- include "laravel-kubernetes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "laravel-kubernetes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
